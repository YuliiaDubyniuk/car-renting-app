import styled from "styled-components";

export const StyledList = styled.ul`
display: flex;
gap: 50px 29px;
flex-wrap:wrap;
margin-bottom: 100px;

`

export const StyledBtn = styled.ul`
  display: block;
  color: #3470FF;
  font-size: 16px;
  font-weight: 500;
  line-height: 24px;
  text-align: center;
  text-decoration: underline;
  background-color: transparent;
  border: none;

  &:hover {
    color: #0B44CD;
  }
`
